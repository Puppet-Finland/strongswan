### THIS FILE IS MANAGED BY PUPPET. ANY MANUAL CHANGES WILL GET OVERWRITTEN.

check process strongswan with pidfile <%= scope.lookupvar('strongswan::params::pidfile') %>
    start program = <%= scope.lookupvar('strongswan::params::service_start') %>
    stop program = <%= scope.lookupvar('strongswan::params::service_stop') %>
    alert <%= scope.lookupvar('strongswan::monit::monitor_email') %> with reminder on 480 cycles
